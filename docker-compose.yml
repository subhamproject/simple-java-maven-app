version: '2'
services:
  maven-app:
     image: maven:3.5-jdk-8
     privileged: true
     user: jenkins
     volumes:
       - $WORKSPACE/.m2:$HOME/.m2
       - $WORKSPACE:$WORKSPACE
     entrypoint: $WORKSPACE/build.sh
